#include <stdio.h>

int main() {
    int a[5] = {1, 2, 3, 4, 5};
    int i;
    int index;
    
    
    printf("Enter the index number: ");
        scanf("%d", &index);
    for(i = 0; i < 6; i++){
        if(a[i] != index){
        printf("%d ", a[i]);
        }
    }
    
    return 0;
}
